import React from 'react'

import css_styles from './Footer.module.scss'

export const Footer = (props) => {
    return <footer className={css_styles.app__footer}>
        VManager Demo v0.0.1
    </footer>
}